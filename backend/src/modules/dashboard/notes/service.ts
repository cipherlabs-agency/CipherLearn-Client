import { Note } from "./types";

export default class NotesService {
  async upload(note: Note) {
    try {
    } catch (error) {
      throw error;
    }
  }
  async getAll() {}
  async draft() {}
}
